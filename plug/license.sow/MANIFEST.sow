#!/usr/bin/env ruby

LICENSES = %w[gpl lgpl mit]

argument(:license) do |v|
  raise ArgumentError, "License name required" unless LICENSES.include?(v.to_s.downcase)
  v.upcase
end

manifest do
  copy "META/*", metadir
  copy "*", '.', :cd => argv[:license].downcase 
end

